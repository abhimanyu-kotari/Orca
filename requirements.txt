# =============================================================================
# ORCA — Marine Intelligence Platform
# requirements.txt
#
# Install all dependencies:
#   pip install -r requirements.txt
# =============================================================================

# --- Core UI ---
streamlit>=1.35.0

# --- LLM (google-genai v2+ replaces deprecated google-generativeai) ---
google-genai>=2.22.0

# --- HTTP client (modern, sync + async) ---
httpx>=0.27.0

# --- Environment variables ---
python-dotenv>=1.0.0

# --- Geocoding (location name → lat/lon) ---
geopy>=2.4.1

# --- Geospatial (used from Phase 3 onward, installed now to avoid conflicts) ---
folium>=0.17.0
streamlit-folium>=0.20.0
geopandas>=0.14.4
shapely>=2.0.0

# --- Data manipulation ---
pandas>=2.2.0
numpy>=1.26.0

# --- Plotting ---
plotly>=5.22.0

# --- Language detection & translation (Phase 5) ---
langdetect>=1.0.9
deep-translator>=1.11.4

# --- Vector memory store (Phase 4) ---
chromadb>=0.5.0
sentence-transformers>=3.0.0
